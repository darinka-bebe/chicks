package com.example.chicks

import io.flutter.embedding.android.FlutterActivity

/**
 * Flutter embedding v2 entry point.
 *
 * Plugins are registered once by [FlutterActivity.configureFlutterEngine].
 * Do not call [GeneratedPluginRegistrant.registerWith] manually — that
 * double-registers every plugin and can crash the emulator at startup.
 */
class MainActivity : FlutterActivity()
