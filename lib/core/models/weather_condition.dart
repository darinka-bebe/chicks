/// Normalized weather types for stylist logic and UI.
enum WeatherCondition {
  sunny,
  cloudy,
  rainy,
  snowy,
  windy,
  foggy,
}

/// Time-of-day bucket for outfit context.
enum DayPhase {
  morning,
  afternoon,
  evening,
  night,
}
